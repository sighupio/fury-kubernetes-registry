---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

images:
  - name: goharbor/harbor-portal
    newTag: v2.1.0

resources:
  - deploy.yml
  - svc.yml

configMapGenerator:
  - name: portal
    files:
      - nginx.conf=config/nginx.conf
